@model IEnumerable<RMERP.DAL.Models.Wage_Process>
@using System.Globalization;
@*
    For more information on enabling MVC for empty projects, visit http://go.microsoft.com/fwlink/?LinkID=397860
*@
<div class="row">
    <div class="col-md-12">
        <div>
            @if (Model.Count() > 0)
            {
                <table class="table table-striped custom-table m-b-0">
                    <thead>
                        <tr>
                            <th>Wag Month</th>
                            <th>Attendance</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td><b>@item.WAG_Month.ToString("MMMM", CultureInfo.CreateSpecificCulture("IN")) - @item.WAG_Month.Year.ToString()</b></td>
                                <td>@Html.ActionLink("Attendance", "AttendancesStatus", "WageProcess",new { wagId =item.WAG_Id, WAG_Month = item.WAG_Month})</td>                             
                            </tr>
                        }

                    </tbody>
                </table>
            }
            else
            {
                <p>No Wage Process To Display</p>
            }
        </div>
    </div>
</div>