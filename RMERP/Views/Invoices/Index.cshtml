@model RMERP.DAL.ViewModel.InvoiceMasterVM 
@using static RMERP.DAL.Helpers.ProjectUtils;
@{
    ViewData["Title"] = "Index";
}
@section scripts{
    @if (TempData["message"] != null)
    {
        <script>
            $(document).ready(function () {
            alert('@TempData["message"]');
        });
        </script>
    };
    <script>
        function searchInvoiceList() {
            var CLI_Id = $("#CLI_Id").val();
            var Url = siteUrl + "Invoices/SearchInvoice?CLI_Id=" + CLI_Id;
            $.ajax({
                type: "GET",
                url: Url,
                success: function (data) {
                    $('#divClientList').html(data);
                }
            })
        }
    </script>
}
<div class="row">
    <div class="col-sm-8">
        <h4 class="page-title">Invoices</h4>
    </div>
    <div class="col-sm-4 text-right m-b-30">
        <a href="@Url.Action("AddEditInvoice","Invoices",new { FRM_Id=Model.FRM_Id,INV_Id=0})" class="btn btn-primary rounded"><i class="fa fa-plus"></i> Create New Invoice</a>
    </div>
</div>
<div class="row filter-row">
      <div class="col-sm-3 col-xs-6">
            <div class="form-group form-focus select-focus">
                <label class="control-label">Clients</label>
                @Html.DropDownListFor(m=>m.CLI_Id, new SelectList(ViewBag.ClientList, "CLI_Id", "CLI_Name"), "Select Client", new { @class = "select floating" })
            </div>
        </div>
        <div class="col-sm-3 col-xs-6">
            <a class="btn btn-success btn-block" onclick="searchInvoiceList()"> Search </a>
        </div>
</div>
<div class="row" id="divClientList">
    @{Html.RenderPartial("_InvoiceList", Model);}
</div>

