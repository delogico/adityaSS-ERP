@model IEnumerable<RMERP.DAL.Models.AdminUsers>
@using static RMERP.DAL.App_Code.ProjectUtils;

@{
    ViewData["Title"] = "Admin Users List";
    Layout = "~/Views/Shared/_RMLayout.cshtml";
}

@section scripts{
    @if (TempData["message"] != null)
    {
        <script>
            $(document).ready(function () {
            alert('@TempData["message"]');
        });
        </script>
    };

}
<style>
    .modal-dialog {
        transform: translate(0, -50%);
        top: 30%;
        margin: 0 auto;
    }
</style>
<div>
    <div class="content container-fluid">
        <div class="row">
            <div class="col-sm-8">
                <h4 class="page-title">@ViewData["Title"]</h4>
            </div>
            <div class="col-sm-4 text-right m-b-30">
                <a class="btn btn-primary rounded" href="@Url.Action("AddEditAdminUsers","AdminUsers")"><i class="fa fa-plus"></i> Add New AdminUsers</a>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                @if (Model.Count() > 0)
                {
                    <div>
                        <table class="table table-striped custom-table">
                            <thead>
                                <tr>
                                    <th>Firm</th>
                                    <th>Name</th>                                   
                                    <th>Email</th>                                    
                                    <th>Password</th>
                                    <th>Mobile</th>
                                    <th class="text-right"></th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                <tr>
                                    <td>@(item.FrmId.HasValue ? item.Frm.FrmName: "Super Admin")</td>
                                    <td>@item.AdmFirstName @item.AdmMiddleName @item.AdmLastName</td>
                                   
                                    <td>@item.AdmEmailId</td>
                                    <td>@item.AdmPassword</td>
                                    <td>@item.AdmMobile</td>

                                    <td class="text-right">
                                        <div class="dropdown">
                                            <a href="#" class="action-icon dropdown-toggle" data-toggle="dropdown" aria-expanded="false"><i class="fa fa-ellipsis-v"></i></a>
                                            <ul class="dropdown-menu pull-right">
                                                <li><a href='@Url.Action("AddEditAdminUsers","AdminUsers",new { AdminId=item.AdmId})' title="Edit"><i class="fa fa-pencil m-r-5"></i> Edit</a></li>
                                                <li><a href='@Url.Action("DeleteAdminUsers","AdminUsers",new { AdminId=item.AdmId})' title="Delete"><i class="fa fa-trash m-r-5"></i> Delete </a></li>
                                            </ul>
                                        </div>
                                    </td>
                                </tr>
                                }

                            </tbody>
                        </table>
                    </div>
                }
                else
                {
                    <P>No Admin Users To Display</P>
                }

            </div>
        </div>
    </div>
</div>

