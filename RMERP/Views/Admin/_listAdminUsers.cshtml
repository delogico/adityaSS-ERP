@model IEnumerable<RMERP.DAL.Models.AdminUsers>

<div class="table-responsive">
    <table class="table table-striped custom-table datatable">
        <thead>
            <tr>
                <th style="width:30%;">First Name</th>
                <th>Middle Name</th>
                <th>Last Name</th>
                <th>Email Address</th>
                <th>Mobile No.</th>
                <th>Firm</th>
                <th class="text-right">Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
            <tr>
                <td>
                    <a href="profile.html" class="avatar">D</a>
                    <h2><a href="profile.html">@item.AdmFirstName @*<span>Admin</span>*@</a></h2>
                </td>
                <td>@item.AdmMiddleName</td>
                <td>@item.AdmLastName</td>
                <td>@item.AdmEmailId</td>
                <td>@item.AdmMobile</td>
                <td>@item.Frm.FrmName</td>
                @*<td>
            <span class="label label-danger-border">Admin</span>
        </td>*@
                <td class="text-right">
                    <div class="dropdown">
                        <a href="#" class="action-icon dropdown-toggle" data-toggle="dropdown" aria-expanded="false"><i class="fa fa-ellipsis-v"></i></a>
                        <ul class="dropdown-menu pull-right">
                            <li><a data-toggle="modal" onclick="onDeleteShow(@item.AdmId)"><i class="fa fa-trash-o m-r-5"></i> Delete</a></li>
                            <li><a onclick="onAddClick(@item.AdmId)" data-toggle="modal"><i class="fa fa-pencil-o m-r-5"></i> Edit</a></li>
                        </ul>
                    </div>
                </td>
            </tr>
            }

        </tbody>
    </table>
</div>
<div id="delete_user"  class="modal custom-modal fade" role="dialog">
    <div class="modal-dialog">
        <div class="modal-content modal-md">
            <div class="modal-header">
                <h4 class="modal-title">Delete Admin User</h4>
            </div>
            <form>
                <div class="modal-body card-box">
                    <p>Are you sure want to delete this?</p>

                    <div class="m-t-20">
                        <a href="#" class="btn btn-default" data-dismiss="modal">Close</a>
                        <button type="submit" id="dltAdminBtn" class="btn btn-danger">Delete</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>
<script type="text/javascript" src="~/assets/js/app.js"></script>

